__pycache__/
*.pyc
supabase/.temp/
.mcp.json

# Phase C3: point-in-time Parquet store (scripts/pit_store.py). Regenerated by
# scripts/pit_seed.py (local) / the eventual C4 backfill workflow, same
# treatment as the data/*.json digests above -- derived data, not hand-edited,
# not meaningfully diffable in git. See docs/C3_DESIGN.md §2.
data/pit/
